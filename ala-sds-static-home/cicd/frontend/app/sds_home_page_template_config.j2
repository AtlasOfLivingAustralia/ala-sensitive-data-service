{
  "Parameters" : {
    "pHomePageBucketPath"  : "{{ home_page_bucket_path }}",
    "pomePageSubDomain"    : "{{ home_page_sub_domain }}"
    "pCleanBranch"         : "{{ clean_branch }}",
    "pXmlBucketPath"       : "{{ xml_file_bucket_path }}",
    "pXmlubDomain"         : "{{ xml_file_sub_domain }}"
    "pEnvironment"         : "{{ environment }}",
    "pHostedZone"          : "{{ hosted_zone }}",
    "pProductName"         : "{{ product_name }}",
    "pSourceBucket"        : "{{ source_bucket }}",
    "pSslCertificate"      : "{{ ssl_certificate }}",
  },
  "Tags" : {
    "product"     : "{{ product_name }}",
    "component"   : "{{ product_component }}",
    "environment" : "{{ environment }}",
    "branch"      : "{{ src_branch }}",
    "version"     : "{{ commit_id }}",
    "build"       : "{{ codebuild_build_number }}"
  },
  "StackPolicy" : {
    "Statement" : [
      {
        "Effect" : "Allow",
        "Action" : "Update:*",
        "Principal": "*",
        "Resource" : "*"
      }
    ]
  }
}
